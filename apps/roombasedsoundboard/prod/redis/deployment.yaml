apiVersion: apps/v1
kind: Deployment
metadata:
  name: rmbsndb-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rmbsndb-redis
  template:
    metadata:
      labels:
        app: rmbsndb-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: rmbsndb-redis-data
