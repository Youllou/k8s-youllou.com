apiVersion: v1
kind: PersistentVolume
metadata:
  name: swinceomatik-mysql-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-rwx
  nfs:
    server: 10.42.0.1
    path: /srv/nfs/SwinceOMatik
